# Face Verification Service - Production env
ENV=production
DEBUG=0

# Database (PostgreSQL recommended for production)
DATABASE_URL=postgresql+psycopg2://user:password@host:5432/face_verify

# Where to store verified images (path on server; for S3 use custom storage later)
UPLOAD_DIR=uploads

# CORS - comma-separated origins (Node.js app + frontend URLs)
CORS_ORIGINS=https://your-node-app.com,https://your-frontend.com

# Optional: API key for Node.js -> this service (set same in Node app)
# API_KEY=your-secret-api-key

# Optional
MAX_IMAGE_SIZE_MB=10
